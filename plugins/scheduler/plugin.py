manifest = {
    "name": "Scheduler Plugin",
    "version": "1.0.0",
    "description": "Task scheduling engine",
    "enabled": True,
}

config = {
    "max_concurrent_tasks": 10,
    "default_timezone": "UTC",
}
